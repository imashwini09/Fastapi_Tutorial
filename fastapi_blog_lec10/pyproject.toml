# FastAPI Blog Application with Authorization
# Project configuration with dependencies for JWT authentication, password hashing, and async database
# Authorization features: JWT tokens, OAuth2, password hashing, session management

[project]
name = "fastapi-blog-lec10"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiosqlite>=0.22.1",
    "fastapi[standard]>=0.135.3",
    "greenlet>=3.5.1",
    "pwdlib[argon2]>=0.3.0",
    "pydantic>=2.13.1",
    "pydantic-settings>=2.14.1",
    "pyjwt>=2.13.0",
    "sqlalchemy>=2.0.49",
]
